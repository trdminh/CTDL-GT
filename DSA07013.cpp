#include <bits/stdc++.h>

using namespace std;

bool Var(char c)
{
    if(c >= 0 && c <= 9)
    {
        return 1;
    }
    return 0;
}
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        /* code */
        string s;
        cin >> s;
    }
    
}