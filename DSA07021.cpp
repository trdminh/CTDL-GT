#include <bits/stdc++.h>


using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        /* code */
        string s;
        cin >> s;
        stack <char> st;
        int cnt = 1, cnt_max = 0;
        for(int i = 1; i < s.size(); i++)
        {
            if(s[0] == ')')
            {

            }
        }
    }
    
}